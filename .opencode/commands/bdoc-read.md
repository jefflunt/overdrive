---
description: Read the build_docs README
agent: plan
---

# Read the `build_docs` README
Run the `Command` section below to get a high-level overview of the current project. Focus on only reading related markdown files, and ignore everything else (we can lazy-load additional resources as needed).

When you're done, produce a summary of the project's purpose, main code layout, and goals.

## Command

Here is a high-level summary of this project:
!`cat build_docs/README.md`
